[package]
name = "anchor-policy"
version = "0.1.0"
edition = "2021"
publish = false
license = "MIT"

[dependencies]
soroban-sdk = { version = "=26.0.1", default-features = false, features = ["alloc"] }

